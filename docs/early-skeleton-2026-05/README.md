# Early GitHub skeleton (2026-05-18)

These files are the original Week 1–4 skeleton that was uploaded to
`github.com/dugu01/indoor-autonomous-drone` on 2026-05-18, before the project
grew into the staged S1–S2.4 simulation now in `simulation/`.

They are kept here only for reference:

| File | Note |
| ---- | ---- |
| `README_week1-4_skeleton.md` | Original project README. Contains an F330 (720 g) hardware parameter table and a simulation-gain → ArduPilot parameter mapping that the current README does not carry. |
| `run_S1_simulation_v1.m` / `cascaded_pid_v1.m` | Earlier versions of the S1 scripts; superseded by `simulation/S1_dynamics_pid/`. |
| `run_S2_visual_slam.m` | Week-2 monoVisualSLAM + `insfilterErrorState` script; superseded by `simulation/S2_visual_slam/run_S2_lidar_slam.m`. |
